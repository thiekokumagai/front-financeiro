---
phase: "03-listagem-produtos-menu-secoes"
plan: "03-02"
type: feature
wave: 1
depends_on: []
files_modified:
  - src/components/settings/DeliverySettingsForm.tsx
  - src/components/settings/PaymentSettingsForm.tsx
  - src/components/settings/GeneralSettingsForm.tsx
  - src/pages/SettingsPage.tsx
  - src/App.tsx
autonomous: true
must_haves:
  truths:
    - "Uma rota /configuracoes exibe três formulários separados na mesma página"
    - "Rotas /entregas e /pagamentos redirecionam para /configuracoes"
  artifacts:
    - src/components/settings/DeliverySettingsForm.tsx
    - src/components/settings/PaymentSettingsForm.tsx
    - src/components/settings/GeneralSettingsForm.tsx
    - src/pages/SettingsPage.tsx
---

# Plan 03-02: Página unificada de Configuração

## Objective
Consolidar Entregas, Pagamentos e Configurações gerais em `/configuracoes` com formulários independentes e salvamento por seção.

## Tasks

<task id="1" title="Extrair formulários das páginas atuais">
<read_first>
- src/pages/DeliveriesPage.tsx
- src/pages/PaymentsPage.tsx
- src/pages/SettingsPage.tsx
- .planning/phases/03-listagem-produtos-menu-secoes/CONTEXT.md
</read_first>

<action>
Criar em `src/components/settings/`:

1. `DeliverySettingsForm.tsx` — mover JSX/estado de `DeliveriesPage` (origem, tabela distância/valor, alerta 32 km). Exportar componente com botão **Salvar** próprio chamando handler `onSave` ou `console.log` placeholder igual ao atual.

2. `PaymentSettingsForm.tsx` — mover de `PaymentsPage` (PIX, parcelas/juros).

3. `GeneralSettingsForm.tsx` — mover de `SettingsPage` (logo, banner, horários, dados loja).

Cada form em `Card` com título de seção: "Entregas", "Pagamentos", "Configurações gerais".
</action>

<acceptance_criteria>
- Três arquivos existem em `src/components/settings/`
- Cada componente exporta função nomeada e renderiza botão "Salvar"
- Comportamento funcional idêntico ao das páginas originais (estado local preservado)
</acceptance_criteria>
</task>

<task id="2" title="Montar SettingsPage unificada">
<read_first>
- src/components/settings/DeliverySettingsForm.tsx
- src/components/settings/PaymentSettingsForm.tsx
- src/components/settings/GeneralSettingsForm.tsx
- src/pages/SettingsPage.tsx
</read_first>

<action>
Reescrever `SettingsPage.tsx`:
- Título da página: **Configuração**
- Layout `space-y-8` empilhando os três forms na ordem: Entregas → Pagamentos → Configurações gerais
- Sem abas; scroll vertical único
</action>

<acceptance_criteria>
- `SettingsPage.tsx` importa os três forms de `@/components/settings/`
- Página não contém lógica duplicada dos forms antigos inline
</acceptance_criteria>
</task>

<task id="3" title="Redirects e limpeza de rotas">
<read_first>
- src/App.tsx
- src/pages/DeliveriesPage.tsx
- src/pages/PaymentsPage.tsx
</read_first>

<action>
Em `App.tsx`:
- Manter rota `/configuracoes` → `SettingsPage`
- Substituir rotas `/entregas` e `/pagamentos` por `<Navigate to="/configuracoes" replace />` (importar de `react-router-dom`)
- Remover imports lazy/direct de `DeliveriesPage` e `PaymentsPage` se não usados

Opcional: deletar `DeliveriesPage.tsx` e `PaymentsPage.tsx` ou deixar arquivos vazios deprecated — preferir deletar se sem referências.
</action>

<acceptance_criteria>
- `App.tsx` contém `Navigate` para `/configuracoes` nas rotas `/entregas` e `/pagamentos`
- `npm run build` no projeto admin-wonderland conclui sem erro de import órfão
</acceptance_criteria>
</task>

## Verification
- [ ] Acessar `/configuracoes` — três seções visíveis com Salvar independente
- [ ] Acessar `/entregas` e `/pagamentos` — redireciona para `/configuracoes`
