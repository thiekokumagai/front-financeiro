---
phase: "03-listagem-produtos-menu-secoes"
plan: "03-01"
type: feature
wave: 1
depends_on: []
files_modified:
  - src/data/admin-nav.ts
  - src/components/admin/AdminSidebar.tsx
autonomous: true
must_haves:
  truths:
    - "Sidebar exibe Dashboard solto, depois seções Catálogo, Vendas e Configuração com labels"
    - "Menu Configuração tem um único link para /configuracoes"
  artifacts:
    - src/data/admin-nav.ts
    - src/components/admin/AdminSidebar.tsx
---

# Plan 03-01: Menu lateral em seções

## Objective
Refatorar `AdminSidebar` para navegação agrupada conforme CONTEXT.md, reutilizando o módulo `Sidebar` existente.

## Tasks

<task id="1" title="Criar configuração de navegação">
<read_first>
- .planning/phases/03-listagem-produtos-menu-secoes/CONTEXT.md
- src/components/admin/AdminSidebar.tsx
- src/components/ui/sidebar.tsx
</read_first>

<action>
Criar `src/data/admin-nav.ts` exportando:

1. `dashboardNavItem`: `{ title: "Dashboard", url: "/", icon: LayoutDashboard }`
2. `navSections`: array com:
   - `{ label: "Catálogo", items: [Produtos /produtos, Categorias /categorias, Variações /variacoes, Cupons /cupons] }`
   - `{ label: "Vendas", items: [Pedidos /pedidos, Caixa /caixa] }`
   - `{ label: "Configuração", items: [Configuração /configuracoes] }`

Usar ícones `lucide-react` já usados no sidebar atual (`Package`, `FolderTree`, `SlidersHorizontal`, `Ticket`, `ShoppingBag`, `Landmark`, `Settings`).
</action>

<acceptance_criteria>
- `src/data/admin-nav.ts` existe e exporta `dashboardNavItem` e `navSections`
- `navSections` tem exatamente 3 seções com labels "Catálogo", "Vendas", "Configuração"
- Seção Configuração contém apenas 1 item com `url: "/configuracoes"`
</acceptance_criteria>
</task>

<task id="2" title="Renderizar grupos no AdminSidebar">
<read_first>
- src/data/admin-nav.ts
- src/components/admin/AdminSidebar.tsx
- src/components/ui/sidebar.tsx
</read_first>

<action>
Em `AdminSidebar.tsx`:
- Remover array `menuItems` inline
- Importar `dashboardNavItem` e `navSections` de `@/data/admin-nav`
- Renderizar `dashboardNavItem` em `SidebarGroup` **sem** `SidebarGroupLabel`
- Para cada entrada em `navSections`, renderizar `SidebarGroup` com `SidebarGroupLabel` (texto da label) e `SidebarMenu` com os itens
- Manter `NavLink`, estados `activeClassName`, collapse com `useSidebar`, logo Pod & Mais
- Não incluir links para `/entregas` ou `/pagamentos` no menu
</action>

<acceptance_criteria>
- `AdminSidebar.tsx` não contém array `menuItems` hardcoded
- `grep -n "Entregas\|Pagamentos" src/components/admin/AdminSidebar.tsx` não retorna entradas de menu
- Build TypeScript sem erros em `AdminSidebar.tsx`
</acceptance_criteria>
</task>

## Verification
- [ ] Navegar manualmente: cada link do menu abre a rota correta
- [ ] Item ativo destaca com `bg-sidebar-accent`
- [ ] Sidebar colapsada mostra ícones sem quebrar layout
